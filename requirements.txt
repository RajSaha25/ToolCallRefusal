# Python 3.12. Tested on an A100-80GB (RunPod).
#
# torch is expected to be preinstalled with a CUDA build that matches your GPU.
# On the RunPod image used here, torch 2.8.0+cu128 ships in the base image, so it is
# intentionally left unpinned below — install it yourself only if your environment
# does not already have it (see https://pytorch.org for the right CUDA wheel).
#
# Install everything else with:
#   python3.12 -m pip install --break-system-packages -r requirements.txt
#
# (--break-system-packages is needed on Debian/RunPod base images that mark the system
#  Python as externally managed; drop it if you are inside a virtualenv.)

transformers>=4.44
accelerate>=0.33
pandas>=2.0
numpy>=1.26
matplotlib>=3.7
openpyxl>=3.1
safetensors>=0.4
sentencepiece>=0.2
huggingface_hub>=0.24
anthropic>=0.40
pytest>=8.0
